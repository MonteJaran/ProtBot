<?xml version="1.0" encoding="utf-8"?>
<!--
  Requests the minimum an app blocker can work with.

  canRetrieveWindowContent is NOT set. ProtBot only needs to know which app is
  in front, and asking for content it does not use would be both a larger
  privacy surface and much harder to justify in Play review.

  typeWindowStateChanged is the only event type requested, for the same reason.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagDefault"
    android:canRetrieveWindowContent="false"
    android:description="@string/blocker_service_description"
    android:notificationTimeout="100" />
